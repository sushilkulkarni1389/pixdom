# OpenSpec session diffs (optional — commit if you want an audit trail)
.claude/SESSION-DIFF-*.md

node_modules/

# Build output — never commit compiled files
dist/
apps/*/dist/
packages/*/dist/
*.tgz
.claude/
dist-tarball/
